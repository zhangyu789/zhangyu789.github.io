{
  "name": "zhangyu789-github-io",
  "private": true,
  "type": "module",
  "scripts": {
    "generate-images": "node scripts/generate-missing-images.mjs",
    "generate-images:dry": "node scripts/generate-missing-images.mjs --dry-run",
    "lint": "eslint \"js/app.js\" \"js/core/**/*.js\" \"js/modules/**/*.js\" \"tests/**/*.js\" --ext .js,.mjs",
    "test": "vitest run"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.2.0",
    "sharp": "^0.33.5",
    "vitest": "^4.1.4"
  }
}
